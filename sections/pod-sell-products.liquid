{{ 'pod.css' | asset_url | stylesheet_tag }}
<div
    class="wemustpod-sell-section"
    style="
      --sell-section-background: {{ section.settings.background }};
      --sell-section-padding-top: {{ section.settings.padding_top }}px;
      --sell-section-padding-bottom: {{ section.settings.padding_bottom }}px;
    "
  >
    <div class="container">
      <div class="wemustpod-sell-header">
        {% if section.settings.eyebrow != blank %}
          <p class="wemustpod-sell-eyebrow">
            {{ section.settings.eyebrow }}
          </p>
        {% endif %}
        {% if section.settings.heading != blank %}
          <h2 class="wemustpod-sell-title">
            {{ section.settings.heading }}
          </h2>
        {% endif %}
        {% if section.settings.description != blank %}
          <div class="wemustpod-sell-text rte">
            {{ section.settings.description }}
          </div>
        {% endif %}
      </div>
      <div class="wemustpod-sell-grid">
        {% for block in section.blocks %}
          {% case block.type %}
          {% when 'product' %}
          <article
            class="wemustpod-sell-card"
            {{ block.shopify_attributes }}
          >
            {% if block.settings.image != blank %}
              {{
                block.settings.image
                | image_url: width: 308
                | image_tag:
                  loading: 'lazy',
                  class: 'wemustpod-sell-image',
                  alt: block.settings.alt
              }}
            {% else %}
              {{ 'image' | placeholder_svg_tag }}
            {% endif %}
          </article>
          {% endcase %}
        {% endfor %}
      </div>
    </div>
</div>
{% schema %}

{
  "name": "POD Sell Products",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow",
      "default": "What you can sell"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "More products. Less vendor management."
    },
    {
      "type": "richtext",
      "id": "description",
      "label": "Description",
      "default": "<p>When you order online, you get access to top-selling print-on-demand products with no minimums, fast 24–48 hour production on select items, and shipping options across the US and Canada.</p>"
    },
    {
      "type": "color",
      "id": "background",
      "label": "Background",
      "default": "#ffffff"
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "Padding Top",
      "min": 0,
      "max": 200,
      "step": 4,
      "unit": "px",
      "default": 80
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "Padding Bottom",
      "min": 0,
      "max": 200,
      "step": 4,
      "unit": "px",
      "default": 80
    }
  ],

  "blocks": [
    {
      "type": "product",
      "name": "Product Card",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Product Image"
        },
        {
          "type": "text",
          "id": "alt",
          "label": "Alt Text",
          "default": "Product"
        }
      ]
    }
  ],

  "presets": [
    {
      "name": "POD Sell Products",
      "blocks": [
        {
          "type": "product",
          "settings": {
            "alt": "Apparel Via DTF"
          }
        },
        {
          "type": "product",
          "settings": {
            "alt": "DTG Apparel"
          }
        },
        {
          "type": "product",
          "settings": {
            "alt": "Promotional Sticker Sets"
          }
        },
        {
          "type": "product",
          "settings": {
            "alt": "Promotional Magnets"
          }
        },
        {
          "type": "product",
          "settings": {
            "alt": "Embroidery Patches"
          }
        },
        {
          "type": "product",
          "settings": {
            "alt": "Drinkware"
          }
        },
        {
          "type": "product",
          "settings": {
            "alt": "Embroidery Patch Keychains"
          }
        },
        {
          "type": "product",
          "settings": {
            "alt": "Wall Stickers"
          }
        },
        {
          "type": "product",
          "settings": {
            "alt": "One-Off Embroidery"
          }
        }
      ]
    }
  ]
}

{% endschema %}